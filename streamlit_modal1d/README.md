\# App Streamlit — Matrizes A e B (Modal 1D)



Etapa 1 do curso: monta \*\*A\*\* e \*\*B\*\* para guia planar 1D (n camadas).

\- Entradas: n\_camadas, larguras, índices n, Np, λ

\- Saídas: A, B, checks e prévias



\## Rodar local

```bash

pip install -r requirements.txt

streamlit run app.py



